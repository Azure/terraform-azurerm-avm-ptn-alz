# Disable a Policy Assignment

This example shows how to disable a specific policy assignment using the `policy_assignments_to_modify` variable with `enabled = false`.

This is useful when you want to deploy the ALZ architecture but exclude certain policy assignments that are not relevant to your environment.
